int main(void) {
  EncDouble ctxt1, ctxt2;
  EncDouble[] ctxt_arr1, ctxt_arr2;
  double x, y;
  double[] arr;
  int i;
  i = 0;
  x = 0.5;
  y = 4.5;
  arr = { 0.5, 0.6, 0.7 }; // array
  ctxt1 = { 1.2, 1.3, 1.4 }; // batching
  ctxt_arr1 = { 0.5, 0.6, 0.7 }; // encrypted array

  ctxt_arr2 = new EncDouble[2];
  ctxt_arr2[i] = { 0.1, 0.2, 0.3 }; // encrypted array with batching
  ctxt_arr2[1] = { 0.4, 0.5, 0.6 }; // encrypted array with batching

  ctxt1++;
  print_batched(ctxt1, 3);

  ctxt1 = ctxt1 - x;
  ctxt1 = y + ctxt1;
  ctxt2 = ctxt1 + ctxt1;
  ctxt2 -= x;

  ctxt_arr2[0] = 10.5;
  ctxt_arr2[1] = (ctxt_arr2[0]) * 2;
  print(ctxt1);
  print(ctxt2);
  print((arr[0]));
  print((ctxt_arr1[0]));
  print((ctxt_arr2[1]));
  print_batched((ctxt_arr2[0]), 3);
  return 0;
}
